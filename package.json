{
  "name": "ionos-wordpress-blueprints",
  "dependencies": {
    "@wordpress/env": "^10.24.0",
    "@wordpress/scripts": "^30.17.0"
  },
  "scripts": {
    "start": "pnpm run wp-env start --xdebug | grep -vwi xdebug",
    "stop": "pnpm run wp-env stop",
    "build": "rm -f build/server.phar && docker run -i --rm -v $(pwd):/app --user $(id -u):$(id -g) -w /app php:8.3-alpine php -d phar.readonly=0 ./scripts/build.php",
    "test": "./scripts/test.sh",
    "destroy": "echo 'y' | pnpm run wp-env destroy",
    "wp-env" : "WP_ENV_HOME=./wp-env-home wp-env",
    "apiserver": "./scripts/apiserver.sh",
    "apiclient": "./scripts/apiclient.sh"
  }
}
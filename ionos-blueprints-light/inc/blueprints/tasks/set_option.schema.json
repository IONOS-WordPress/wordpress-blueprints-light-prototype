{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "description": "Structure of the set_option task payload.",
  "properties": {
    "id": {
      "type": "string",
      "description": "The unique id of this task.",
      "format": "uuid"
    },
    "type": {
      "type": "string",
      "description": "The type of this task.",
      "const": "set_option"
    },
    "args" : {
      "type": "object",
      "properties" : {
        "name": {
          "type": "string",
          "description": "The name of the wordpress option to set."
        },
        "value": {
          "type": "string",
          "description": "The value of the wordpress option to set."
        },
        "autoload": {
          "type": ["string", "null"],
          "default": null,
          "description": "The autoload value of the wordpress option to set. If null, the autoload value will not be set."
        }
      },
      "required": ["name", "value"]
    }
  },
  "required": ["args", "id", "type"]
}
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "description": "This schema defines the structure of the set_option job payload.",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the wordpress option to set."
    },
    "value": {
      "type": "string",
      "description": "The value of the wordpress option to set."
    },
    "autoload": {
      "type": ["string", "null"],
      "default": null,
      "description": "The autoload value of the wordpress option to set. If null, the autoload value will not be set."
    }
  },
  "required": ["name", "value"]
}
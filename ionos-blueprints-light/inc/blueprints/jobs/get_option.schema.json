{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "description": "Structure of the get_option job payload.",
  "properties": {
    "id": {
      "type": "string",
      "description": "The unique id of this job.",
      "format": "uuid"
    },
    "type": {
      "type": "string",
      "description": "The type of this job.",
      "const": "set_option"
    },
    "args" : {
      "type": "object",
      "properties" : {
        "name": {
          "type": "string",
          "description": "The name of the wordpress option to get."
        }
      },
      "required": ["name"]
    }
  },
  "required": ["args", "id", "type"]
}
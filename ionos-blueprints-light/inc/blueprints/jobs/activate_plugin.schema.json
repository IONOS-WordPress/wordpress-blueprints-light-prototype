{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "description": "This schema defines the structure of the activate_plugin job payload.",
  "properties": {
    "id": {
      "type": "string",
      "description": "The unique id of this job.",
      "format": "uuid"
    },
    "type": {
      "type": "string",
      "description": "The type of this job.",
      "const": "activate_plugin"
    },
    "args" : {
      "type": "object",
      "properties" : {
        "slug": {
          "type": "string",
          "description": "The slug of the wordpress plugin to install or an https url to download the plugin from."
        },
        "force": {
          "type": "boolean",
          "default": false,
          "description": "If true, the plugin will uninstall a potentially existing instance of this plugin"
        }
      },
      "required": ["slug"]
    }
  },
  "required": ["args", "type", "id"]
}
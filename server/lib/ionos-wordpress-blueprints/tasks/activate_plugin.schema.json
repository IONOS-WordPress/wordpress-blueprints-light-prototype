{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "description": "Structure of the activate_plugin task payload.",
  "properties": {
    "id": {
      "type": "string",
      "description": "The unique id of this task.",
      "format": "uuid"
    },
    "type": {
      "type": "string",
      "description": "The type of this task.",
      "const": "activate_plugin"
    },
    "args" : {
      "type": "object",
      "properties" : {
        "slug": {
          "type": "string",
          "description": "The slug of the wordpress plugin to install or an https url to download the plugin from."
        },
        "force": {
          "type": "boolean",
          "default": false,
          "description": "If true, the plugin will not throw an error if the plugin was already activated or is not installed"
        }
      },
      "required": ["slug"]
    }
  },
  "required": ["args", "type", "id"]
}